# Java Traceability

- Java is the first substantial follower port of the expanded Rust baseline.
- Every generator family now carries a traceability row back to Rust and `stakeholder-core`.
- Missing behavior belongs in `GAPS.md` with feature-level precision.
- The remaining explicit Java gap is the experimental live-provider runtime, which stays out of deterministic parity in this wave.
